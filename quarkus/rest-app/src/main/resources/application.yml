quarkus:
  swagger-ui:
    always-include: true
  http:
    cors:
      ~: true
      methods: GET,POST,OPTIONS
      origins: "/.*/"
    port: 9000

  ssl:
    native: true

mp:
  openapi:
    filter: "io.openepcis.resources.oas.EPCISExampleOASFilter"
